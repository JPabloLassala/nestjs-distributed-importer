describe('TrackService', () => {
  describe('getters', () => {
    it('Should return all tracks');

    it('Should return tracks in album');

    it('Should return tracks from band');

    it('Should return tracks from playlist');
  });
});
