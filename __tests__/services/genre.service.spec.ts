describe('AlbumService', () => {
  describe('getters', () => {
    it('Should return all genres', () => {});

    it('Should return genres in band', () => {});

    it('Should return song genre', () => {});
  });
});
